#include <iostream>
#include <map>

bool addConnection(int mem1, int mem2, std::map<int, bool>& isConnectedToRoot) {

}

int main() {
    int totalNumberOfMembers(0);
    int currentNumberOfMembers(0);
    std::map<int, bool> isConnectedToRoot;

    if (isConnectedToRoot[member]) {

    }
    return 0;
}